phpstorm运行配php程序，需要配置：语言与框架--php-interpreter(注释器）
phpstorm 按住Ctrl键，可显示函数用法

鼠标放在右边可查看前面或者后面代码

方便之处可以根据类名，查找类文件所在路径

调试php文件老出问题，尽量用其他服务器集成包

选中类名--出现show diagram(类继承图表)

ctrl+shift+f项目文件查找--打开路径查找,在en输入法下

ctrl+N查找类
 
ctrl+shift+N查找文件

alt+向下箭头，循环显示类中方法。

diagram窗口，按住alt键显示放大镜

窗口切换alt+ctrl+[

tab闭合网页标签，！+tab生成HTML代码

Ctrl+shift+N  查找文件

文件红线多为有错误--多为不小心碰到键盘

Shift + F6  Rename

编辑->查找->路径查找

Ctrl+shift+A执行软件操作

添加TODO注释

win7最小化当前窗口--先按Alt+space,然后按N

win7 最小化全部窗口：win+d/win+m

运行->切换行断点   ctrl+F8，支持.php文件，不支持html 

tab    代码增加缩进

tab+shift  代码减少缩进

hex 十六进制

Octal八进制

F5复制文件 

ctrl+shift+J 将多行合并到一行

Ctrl+G 跳转到代码行

ctrl+H 打开继承关系

Ctrl+R 打开替换对话框

文件夹右键->标记目录

phpstorm窗口，右边黄色快代表有警告错误     

ctrl+c 复制光标所在代码行

alt+ctrl+]切换phpstorm窗口

ctrl+tab打开切换窗口

ALT+方向键tab窗口切换

将鼠标放在图片src路径上，按住Shift键可显示图片缩略图

ctrl+shift+z重做

ctrl+shift+/添加块注释

Ctrl+shift+J连接行

unreachable statement错误多为上面代码有return，执行不到

ctrl+D复制所选内容，换行

代码--自动缩进排版（需选中）

按住alt键，可添加多个鼠标点。

为函数参数添加注释，选择菜单：代码-生成-PHPdoc 注释/右键--生成

——————————————————————
/*选择多处复制，按住alt*/

{*ctrl+alt+I自动缩进排版*}


<!--选中代码，按住ALT+shift，可进行拖拽到合适位置-->


/*点击文件头鼠标滚动显示隐藏文件tab窗口*/


F6移动文件

/* ctrl+alt+左箭头 ：后退*/

/* ctrl+alt+右箭头 ：前进*/


 /*ctrl+F12打开文件结构对话框，可以跳转到相应方法*/
